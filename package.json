{
  "name": "cashew-budget",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Application budgetaire inspiree de Cashew — suivi des depenses, budgets et comptes, 100% locale.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "esbuild scripts/smoke.mjs --bundle --platform=node --format=cjs --jsx=automatic --loader:.mjs=jsx --outfile=dist/smoke.cjs && node dist/smoke.cjs"
  },
  "dependencies": {
    "lucide-react": "^0.469.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11"
  }
}
